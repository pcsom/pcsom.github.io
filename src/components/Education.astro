---
import { siteConfig } from "../config";
const hasEducation = siteConfig.education && siteConfig.education.length > 0;
---

{
  hasEducation && (
    <section id="education" class="p-8 sm:p-12 md:p-16 lg:p-24">
      <div>
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
          <div class="lg:col-span-4">
            <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-gray-900">
              Education
            </h2>
            <div
              class="w-[75px] h-[5px] mt-2 rounded-full"
              style={`background-color: ${siteConfig.accentColor}`}
            />
          </div>

          <div class="lg:col-span-8">
            <div class="space-y-8">
              {siteConfig.education.map((edu) => (
                <div class="bg-gradient-to-br from-slate-50 to-gray-50 rounded-2xl shadow-sm border border-gray-200 p-6 sm:p-8 md:p-10 transition-all duration-500 hover:shadow-xl hover:from-slate-100 hover:to-gray-100 hover:border-slate-300 hover:-translate-y-1">
                  <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between mb-6">
                    <div class="flex-1">
                      <h3 class="text-xl sm:text-2xl font-bold text-gray-900 mb-2">
                        {edu.degree}
                      </h3>
                      <p
                        class="text-lg sm:text-xl font-semibold mb-1"
                        style={`color: ${siteConfig.accentColor}`}
                      >
                        {edu.school}
                      </p>
                    </div>
                    <span class="text-sm sm:text-base text-gray-500 mt-2 sm:mt-0 px-3 py-1 bg-gray-100 rounded-full font-medium">
                      {edu.dateRange}
                    </span>
                  </div>

                  {edu.achievements.length > 0 && (
                    <ul class="space-y-3">
                      {edu.achievements.map((achievement) => (
                        <li class="flex items-start group/item">
                          <div 
                            class="inline-block w-2 h-2 rounded-full mt-2.5 mr-4 flex-shrink-0 transition-all duration-300 group-hover/item:scale-125" 
                            style={`background-color: ${siteConfig.accentColor}`}
                          />
                          <span class="text-base sm:text-lg text-gray-600 leading-relaxed">
                            {achievement}
                          </span>
                        </li>
                      ))}
                    </ul>
                  )}
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
